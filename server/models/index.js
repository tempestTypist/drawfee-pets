const User = require('./User');
const Pet = require('./Pet');
const AllPets = require('./AllPets');
const Bots = require('./Bots');
const Message = require('./Message');
const Post = require('./Post');

module.exports = { User, Pet, AllPets, Bots, Message, Post };
